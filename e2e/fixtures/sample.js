// Sample JavaScript file for testing
function greet(name) {
  return `Hello, ${name}!`;
}

class Calculator {
  add(a, b) { return a + b; }
  subtract(a, b) { return a - b; }
}

module.exports = { greet, Calculator };
